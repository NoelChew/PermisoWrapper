<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <Button
        android:id="@+id/button_check_notification_settings"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_margin="4dp"
        android:backgroundTint="@color/colorPrimary"
        android:padding="20dp"
        android:text="Check Notification Settings"
        android:textColor="@android:color/white"
        android:textSize="20sp" />

    <Button
        android:id="@+id/button_check_permissions"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_above="@+id/button_check_notification_settings"
        android:layout_margin="4dp"
        android:backgroundTint="@color/colorPrimary"
        android:padding="20dp"
        android:text="Check Permissions"
        android:textColor="@android:color/white"
        android:textSize="20sp" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recycler_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_above="@+id/button_check_permissions"></androidx.recyclerview.widget.RecyclerView>

    <!--<ScrollView-->
    <!--android:layout_above="@+id/button_check_permissions"-->
    <!--android:layout_alignParentTop="true"-->
    <!--android:layout_width="match_parent"-->
    <!--android:layout_height="match_parent">-->
    <!---->
    <!--<LinearLayout-->
    <!--android:layout_width="match_parent"-->
    <!--android:layout_height="wrap_content">-->
    <!---->
    <!--</LinearLayout>-->
    <!---->
    <!--</ScrollView>-->

</RelativeLayout>